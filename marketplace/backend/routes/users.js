import express from 'express';


const router = express.Router();

/*
router.get('/', getUsers);
router.post('/', createUser);
router.get('/productosquevendes, Getproductosquevendes);
router.
*/
export default router;
